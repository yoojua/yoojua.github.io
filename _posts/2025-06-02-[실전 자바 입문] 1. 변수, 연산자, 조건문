---
layout: post
title: "[실전 자바 입문] 1. 변수, 연산자, 조건문"
---
![](https://velog.velcdn.com/images/mymy/post/1a52366c-3efb-4558-8c04-66afbd1840de/image.png)

# 🚩섹션 1. Hello World
```java
public class HelloJava {
    public static void main(String[] args) {
    System.out.println("hello java");
	}
}
```
- HelloJava라는 **클래스**이다.
파일명과 클래스 이름이 같아야한다.
- `{} 블록`을 통해 클래스의 시작과 끝을 나타낸다.
- `public static void main(String[] args)`은 main 메서드 라고 하는데, 자바는 main 메서드를 찾아 프로그램을 시작한다. main 메서드가 프로그램의 시작점이다.
- 자바는 `;(세미콜론)`으로 문장을 구분한다. 문장이 끝나면 세미콜론을 필수로 넣어주어야한다.
<br/>

## 🛠️표준 스펙과 구현
자바는 표준 스펙과 구현으로 나눌 수 있다.

1. 표준 스펙
- 스펙은 설계도이며 문서. 이 스펙을 기반으로 여러 회사에서 자바를 만든다.
- 자바 커뮤니티 프로세스(JCP)를 통해 관리된다.
2. 구현
- 여러 회사에서 자바 표준 스펙에 맞추어 실제 작동하는 자바 프로그램을 개발한다.
- 대부분 윈도우, MAC, 리눅스 같이 다양한 OS에서 작동하는 버전의 자바도 함께 제공한다.

</br>

*️⃣ 일반적인 프로그램은 다른 운영체제에서 실행할 수 없다. 하지만 자바 프로그램은 자바의 운영체제 독립성 덕분에 각각의 환경에 맞추어 자바를 설치할 수 있고, 그렇기에 자바가 설치된 모든 OS에서 실행할 수 있다. 


## 🛠️자바 프로그램 실행 과정
![](https://velog.velcdn.com/images/mymy/post/e064aed3-5f2e-4f32-97a4-6b02a3bb3927/image.png)자바 프로그램은 **컴파일**과 **실행 단계**를 거친다.
1. 자바 소스 코드를 개발자가 작성한다.
2. 자바 컴파일러를 사용해 소스 코드를 컴파일한다.
- 자바가 제공하는 **javac**를 사용해 .java를 .class 파일로 생성한다.
- 소스 코드를 바이트 코드로 변환하며 자바 가상 머신에서 더 빠르게 실행될 수 있게 최적화하고 문법 오류도 검출한다.
3. 자바 프로그램을 실행한다.
- 자바에서 제공하는 **java**라는 프로그램을 사용한다.
- 자바 가상머신(JVM)이 실행되면서 프로그램이 작동한다.

<br/>

### 🤗IDE가 컴파일 & 실행을 대신해준다!
위 javac와 java 프로그램을 IDE가 자동으로 처리해준다.
이때 컴파일 된 `.class` 파일을 지정해주면 된다.
즉, IDE에서 실행버튼을 누르면 컴파일과 실행이 모두 한 번에 처리된다.

<br/>

---

# 🚩섹션 2. 변수
변수란, <span style="color: red">**변할 수 있는 값(데이터)을 담을 수 있는 그릇**</span>이다.
```java
int a = 10;
```
a는 숫자 정수(integer)를 보관할 수 있는 데이터 저장소이다.
- **변수 선언**: 변수를 만드는 것
- **변수 초기화**: 선언한 변수에 처음으로 값을 대이해 저장하는 것

*️⃣ 변수를 초기화 하지 않으면 이상한 값이 출력될 수 있어, 자바는 변수를 초기화하도록 강제한다.

## 1️⃣ 변수 타입
```java
<정수>
int, long : 자바는 정수에 기본으로 int를 사용한다. 만약 20억이 넘을 것 같으면 long을 쓴다.
파일을 다룰 때는 byte를 사용한다.

<실수>
double: 실수는 고민하지 말고 double을 쓰면 된다.

<불린형>
boolean: true, false 참 거짓을 표현한다. 이후 조건문에서 자주 사용된다.

<문자열>
String: 문자를 다룰 때는 문자 하나든 문자열이든 모두 String을 사용하는 것이 편리하다.
```
자주 사용하는 타입을 제외하고 실무에서 나머지를 사용하는 경우는 거의 없다.

```java
byte: 표현 길이가 너무 작다. 
또 자바는 기본으로 4byte(int)를 효율적으로 계산하도록 설계되어 있기에 int를 사용하자.
byte 타입을 직접 선언하고 여기에 숫자 값을 대입해서 계산하는 일은 거의 없다.
대신에 파일을 바이트 단위로 다루기 때문에 파일 전송, 파일 복사 등에 주로 사용된다.

short: 표현 길이가 너무 작다. 또 자바는 기본으로 4byte( int )를 효율적으로 계산하도록
설계되어 있다. int 를 사용하자

float: 표현 길이와 정밀도가 낮다. 
실수형은 double 을 사용하자.float는 숫자 뒤에 f를 붙여줘야한다.
float 5 = 10.0f;

char: 문자 하나를 표현하는 일은 거의 없다. 
문자 하나를 표현할 때도 문자열을 사용할 수 있다.
예를 들어 String a = "b" 와 같이 사용하면 된다.
```
</br>

## *️⃣ 변수 명명 규칙
**규칙**

1. 변수 이름은 숫자로 시작할 수 없다. (예: 1num , 1st )
2. 이름에는 공백이 들어갈 수 없다.
3. 자바의 예약어를 변수 이름으로 사용할 수 없다. (예: int , class , public )
4. 변수 이름에는 영문자( a-z , A-Z ), 숫자( 0-9 ), 달러 기호( $ ) 또는 밑줄( _ )만 사용할 수 있다.
 

**관례**
- 낙타표기법
첫 번째 단어는 소문자로 시작하고 그 이후의 각 단어는 대문자로 시작한다.

**정리**
클래스는 대문자로 시작하고 나머지는 낙타표기법을 사용한다.
ex. 클래스: OrderDetail, 변수: firstName

**예외**
- 상수는 모두 대문자를 사용하고 언더바로 구분한다.
- 패키지는 모두 소문자를 사용한다.

<br/>

---
# 🚩섹션 3. 연산자

```java
연산자의 종류

산술 연산자: + , - , * , / , %
증감(증가 및 감소) 연산자: ++ , --
비교 연산자: == , != , > , < , >= , <=
논리 연산자: && (AND), || (OR), ! (NOT)
대입 연산자: = , += , -= , *= , /= , %=
삼항 연산자: ? :
```
- 증감 연산자는 전위, 후위 증감 연산자로 나뉜다.
전위(++a)는 먼저 값을 증가시키고, 후위(a++)는 마지막에 값을 증가 시킨다.

 
- 비교 연산자를 사용하면 true 또는 false가 나온다.
= 는 대입 연산자(변수에 값을 대입)이고, ==는 동등한지 확인하는 비교 연산자이다.
하지만 문자열이 같은지 비교할때는 .equals() 메서드를 사용해야 한다.
- 논리 연산자는 boolean형인 true , false 를 비교하는데 사용한다.
&& (그리고) : 두 피연산자가 모두 참이면 참을 반환, 둘중 하나라도 거짓이면 거짓을 반환
|| (또는) : 두 피연산자 중 하나라도 참이면 참을 반환, 둘다 거짓이면 거짓을 반환
! (부정) : 피연산자의 논리적 부정을 반환. 즉, 참이면 거짓을, 거짓이면 참을 반환
- 대입 연산자는 값을 변수에 할당하는 연산자인데,
- 축약(복합) 대입 연산자를 사용해 한번에 축약할 수 있다. +=, -= 등..
- 삼항 연산자(조건 연산자)는 `(조건) ? 참표현식 : 거짓 표현식`으로 이루어져 있다.

<br/>

```java
우선순위

1. 괄호 ()
2. 단항 연산자 (예: ++ , -- , ! , ~ , new , (type) )
3. 산술 연산자 ( * , / , % 우선, 그 다음에 + , - )
4. Shift 연산자 ( << , >> , >>> )
5. 비교 연산자 ( < , <= , > , >= , instanceof )
6. 등식 연산자 ( == , != )
7. 비트 연산자 ( & , ^ , | )
8. 논리 연산자 ( && , || )
9. 삼항 연산자 ( ? : )
10. 대입 연산자 ( = , += , -= , *= , /= , %= 등등)
```

<br/>

# 🚩섹션 4. 조건문
조건문: 특정 조건에 따라 다른 코드를 실행하는 것
조건문에는 if문과 swith문이 있다.

1. if문 - if, else
- if문은 특정 조건이 참인지 확인하고, 그 조건이 참일 경우 특정 코드 블록을 실행한다.

```java
if () {
	코드 내용
}
```
( )안이 true이면 코드 내용을 출력한다.
( )안이 false면 실행하지 않고 if문을 빠져나간다.

 

- else문은 if문에서 만족하는 조건이 없을 때 실행한다.

```java
if (condition) {
	// 조건이 참일 때 실행되는 코드
} else {
	// 만족하는 조건이 없을 때 실행되는 코드
}
```
---

2. if문 - else if
- else if문은 앞선 if문의 조건이 거짓일 때 다음 조건을 검사한다.
- 만약 앞선 if문이 참이라면 else if를 실행하지 않는다.

```java
if (condition1) {
	// 조건1이 참일때 실행되는 코드
} else if (condition2) {
	// 조건1이 거짓이고 조건2가 참일때 실행되는 코드
} else if (condition3) {
	// 조건2가 거짓이고, 조건3이 참일때 실행되는 코드
} else {
	// 모든 조건이 거짓일 때 실행되는 코드
}

```
---

3. if문 - if문과 else if문
각각 따로 수행이 되어야할때 if문을 사용한다.

ex1. if-else 사용: 서로 연관된 조건이어서 하나로 묶을 때
```java
if(condition1) {
	// 작업1 수행
} else if (condition2) {
	// 작업2 수행
}
```


ex2. if 각각 사용: 독립 조건일 떄
```java
if(condition1) {
	// 작업1 수행
}
if(condtion2) {
	// 작업2 수행
}
```
 ---

4. switch문
if문을 더 편리하게 사용할 수 있는 기능이 switch문이다.
- 조건식의 결과 값이 어떤 case의 값과 일치하면 해당 case의 코드를 실행한다.

```java
switch (조건식) {
	case value1 :
	// 조건식의 결과 값이 value1일 때 실행되는 코드
		break;
	case value2 :
	// 조건식의 결과 값이 value2일 때 실행되는 코드
		break;
	default:
	// 조건식의 결과 값이 위의 어떤 값에도 해당되지 않을 때 실행되는 코드
}
```
- break문은 현재 실행 중인 코드를 끝내고 switch문을 빠져나가게 하는 역할을 한다. 
만약 break문이 없으면, 일치하는 case이후의 모든 case 코드들이 순서대로 실행된다.
- default는 조건식의 결과값이 모든 case의 값과 일치하지 않을 때 실행된다.
if문의 else와 같다. default 구문은 선택이다.
 <br/>
- switch문은 값이 같은지 확인하는 연산만 가능하다. (문자도 포함)
그래서 특정 값에 따라 코드를 실행할 때 if문 보다 더 간결하게 코드를 작성할 수 있다.

<br/>

---
*️⃣ **java14에 새로운 switch문**
```java
int grade = 2;

int coupon = switch (grade){
    case 1 -> 1000;
    case 2 -> 2000;
    case 3 -> 3000;
    default -> 500;
   };
System.out.println("발급받은 쿠폰 " + coupon);
```
